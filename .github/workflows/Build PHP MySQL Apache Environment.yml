name: Build PHP MySQL Apache Environment

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Print workspace directory
      run: echo "Code is downloaded to $GITHUB_WORKSPACE {$GITHUB_WORKSPACE}"
